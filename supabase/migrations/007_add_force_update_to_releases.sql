-- ================================================================================================
-- 📂 项目：拾光谣 - 应用版本强制更新功能
-- 📝 版本：v1.0 - Force Update Feature
-- 🎯 目标：为 app_releases 表添加强制更新标记
-- 📅 日期：2026-02-02
-- ================================================================================================

-- 添加强制更新字段
alter table public.app_releases
add column if not exists force_update boolean default false;

-- 添加注释
comment on column public.app_releases.force_update is '是否强制更新：true=强制更新（弹窗不可关闭），false=可选更新（弹窗可关闭）';

-- ================================================================================================
-- 完成
-- ================================================================================================

DO $$
BEGIN
  RAISE NOTICE '✅ 强制更新功能已添加！';
  RAISE NOTICE '📊 已为 app_releases 表添加 force_update 字段';
END $$;
